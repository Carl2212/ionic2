

//load the module for our app


describe("HelloIonicPage", function () {
  var controller;
  // TODO:load the component
  //beforeEach(module('HelloIonicPage'));
  // TODO:Instantiate the Controller and Mocks
  //beforeEach(inject(function($controller,$q){
  //  Mock = jasmine.createSpy('');
  //}));
  describe("BASE64PIPE", function () {
    //list name 为当前测试项的提示信息
    it("list name", function () {
      //表达式null 是否等于undefined 表达式。判断两个表达式是否相等。true是为正确。否则返回failures
      expect(null).not.toEqual(undefined);
    });
    it("list name2", function () {
      //表达式null 是否等于undefined 表达式。判断两个表达式是否相等。true是为正确。否则返回failures
      expect(null).not.toEqual(undefined);
    });
  });
});
